//FILTER
let playlist = ["Rap", "Pop", "Trap", "Hip-Hop", "R/B"];

let playlistFiltrada = playlist.filter(musica => musica.length > 4);

console.log(`Playlist filtrada (músicas com mais de 4 letras): ${playlistFiltrada}`);

